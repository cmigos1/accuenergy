.pio
ESP-Network/.vscode/.browse.c_cpp.db*
.ESP-Network/c_cpp_properties.json
.ESP-Network/launch.json
STM-Amostrador/build
*.vscode
.vscode
STM-Amostrador/.settings

# Dados gravados em runtime (medições / harmônicos)
#*.csv
#*.h5

# Helpers de deploy/diagnostico (alguns com credenciais hardcoded) — nunca commitar
python-monitor/_*.py

# Cache do Python
__pycache__/
*.pyc

# Settings do Claude Code — o allowlist registra comandos que podem conter
# credenciais (ex.: senha SSH). Nao commitar.
.claude/settings.json
.claude/settings.local.json

nilmtk/